[[snippet "fragment-postfix"]]
if (v_position.y > 2.5) {
  color *= vec4(0.49, 0.78, 0.31, 1);
}
else {
	color *= vec4(0.92, 0.80, 0.64, 1);
}
color.r = float(int(color.r * 30)) / 30;
color.g = float(int(color.g * 30)) / 30;
color.b = float(int(color.b * 30)) / 30;
[[end]]